# Project instructions

## Generated web assets

- Edit the readable HTML source when changing the web UI.
- Do not regenerate, compare, or validate `.min.html.gz` assets unless the user explicitly requests it or asks to diagnose the asset pipeline.
- The user's flash workflow regenerates the compressed asset automatically; repeating that work during implementation wastes time and tokens.
